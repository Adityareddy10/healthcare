# Railway assigns the port
server.port=${PORT:8081}

# Railway MySQL connection (using MYSQL_URL environment variable)
# Local fallback: jdbc:mysql://localhost:3306/healthcare
spring.datasource.url=${MYSQL_URL:jdbc:mysql://localhost:3306/healthcare}
spring.datasource.username=${MYSQLUSER:root}
spring.datasource.password=AAAK10__3011

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.open-in-view=false
